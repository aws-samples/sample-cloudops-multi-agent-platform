# User-owned config, generated by `make configure` / `make reconfigure-shared`.
# Terraform auto-loads this alongside terraform.tfvars; per-dev contents make
# it unsuitable for version control (different environments, SSM mirror is
# the shared source of truth).
config.auto.tfvars.json
